const NotFound = () => {
  return (
    <div
      style={{
        minHeight: "96vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "large",
      }}
    >
      URL not found
    </div>
  );
};

export default NotFound;
