const LogoutButton = () => {
  return (
    <button
      className={'buttons signup-home'}
      style={{ borderColor: "white", padding: 0 }}
    >
      LOGOUT
    </button>
  );
};

export default LogoutButton;
