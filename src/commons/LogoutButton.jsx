const LogoutButton = () => {
  return (
    <button
      className={'buttons signup-home'}
      style={{ borderColor: "white", paddingTop: "1rem" }}
    >
      LOGOUT
    </button>
  );
};

export default LogoutButton;
